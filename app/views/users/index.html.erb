<% provide(:title, '一覧') %>

<h1>ユーザー一覧</h1>

<%= will_paginate @users, :previous_label => '前へ', :next_label => '次へ' %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate @users, :previous_label => '前へ', :next_label => '次へ' %>
